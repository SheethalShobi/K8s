putting sensitive info inside docker image is not a good idea because containers can be inspected, exported and published publicly.

small amount of sensitive data e.g.: passwd, tokens

risk of exposing sensitive data

injected into pods/containers

stored inside etcd db on k8's master

there is a limit to each secret size

loaded as tmpfs and is available at well defined location

injects secrets into pods:

as Env variables

volumes

each secret is stored in tmpfs

control using RBAC

created using kubectl / manifest file

Secrets are given in encoded format, can’t see when described after creating it.

While config map is visible

Both are similar and can be injected as env or vol
